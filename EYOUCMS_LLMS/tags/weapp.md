# `{eyou:weapp}` — 应用插件

> **用途**：加载网站安装的微应用插件，建议放置在模板文件底部。
> **官方文档**：https://www.eyoucms.com/doc/label/all/580.html

---

## 语法

```html
{eyou:weapp type='default' /}
```

## 参数

| 参数 | 说明 |
|------|------|
| `type` | 固定填 `default` |

> 单标签，直接放于模板底部，自动加载已安装的所有插件。
